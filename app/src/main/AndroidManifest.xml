<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GREENAPP"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".AndroidSmallHomeActivity"
            android:exported="true"
            android:theme="@style/Theme.GREENAPP"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".AndroidSmallLoginActivity"
            android:exported="false"
            android:theme="@style/Theme.GREENAPP">
        </activity>

        <activity
            android:name=".AndroidSmallTriviaActivity"
            android:exported="false"
            android:theme="@style/Theme.GREENAPP">
        </activity>

        <activity
            android:name=".AndroidSmallMapActivity"
            android:exported="false"
            android:theme="@style/Theme.GREENAPP">
        </activity>

        <activity
            android:name=".AndroidSmallProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.GREENAPP">
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <receiver
            android:name=".GeofenceReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.thsst2.greenapp.GEOFENCE_TRANSITION_ACTION" />
            </intent-filter>
        </receiver>

    </application>

</manifest>