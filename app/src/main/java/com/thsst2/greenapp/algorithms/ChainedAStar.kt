package com.thsst2.greenapp.algorithms

import com.thsst2.greenapp.data.PoiEntity

class ChainedAStar {
    // temporary code used only to compile tests
    fun findPath(preferences: List<PoiEntity>): List<PoiEntity> {
        return preferences.toList()
    }
}